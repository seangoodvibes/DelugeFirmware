# Audio Export

#COMMUNITY #GLOBAL #SONG #ARRANGER #KIT

```shortcut
press(SAVE) + press(RECORD)
```

Starts audio export from Session/Arranger, or Kit drum export when you are in Kit Instrument Clip View.

# Velocity View

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(VELOCITY_PATCH_SOURCE)
```

Opens the note velocity editor from Instrument Clip View or Automation View.

# Enter Automation View

#COMMUNITY #AUTOMATION #SONG #ARRANGER #SYNTH #KIT #MIDI #CV

```shortcut
press(CLIP)
```

Opens Automation View from Arranger View or any clip view.

# Toggle Automation Editor from Menu

#COMMUNITY #AUTOMATION #SONG #ARRANGER #SYNTH #KIT #MIDI #CV

```shortcut
press(CLIP)
```

Opens or closes the Automation Editor for the selected parameter while in a Sound menu.

# Automation Overview

#COMMUNITY #AUTOMATION #ARRANGER

```shortcut
press(ENTIRE)
```

Returns from Automation Arranger View to Automation Overview.

# Automation Overview from Clip View

#COMMUNITY #AUTOMATION #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(CLIP)
```

Returns from Automation Clip View to Automation Overview.

# Automation Overview from Clip Pad Shortcut

#COMMUNITY #AUTOMATION #SYNTH #KIT #MIDI #CV

```shortcut
hold(AUDITION) + press(CLIP)
```

Alternative shortcut for returning from Automation Clip View to Automation Overview.

# Clear Automations

#COMMUNITY #AUTOMATION #ARRANGER #SYNTH #KIT #MIDI #CV

```shortcut
hold(X) + press(BACK)
```

Clears all automations in the current clip or row.

# Automation Arranger Auto-Scroll

#COMMUNITY #AUTOMATION #ARRANGER

```shortcut
press(CROSS)
```

Toggles automatic scrolling during playback in Automation Arranger View.

# Select Note Row in Velocity Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
press(AUDITION)
```

Selects the current note row in the velocity editor.

# Select Note Row in Velocity Editor with Vertical Encoder

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
turn(Y)
```

Alternative way to select a different note row in the velocity editor.

# Note Row Probability

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + turn(SELECT)
```

Adjusts the selected note row's probability in the velocity editor.

# Note Row Iterance

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + turn(SELECT)
```

Adjusts the selected note row's iterance in the velocity editor.

# Note Row Velocity

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(GRID) + turn(X)
```

Adjusts the selected note's velocity in the velocity editor.

# Note Row Fill

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(SYNC)
```

Adjusts the selected note row's fill setting when the Community Feature is enabled.

# Note Row Automation

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(GRID) + turn(PARAMETER)
```

Adjusts the selected note's automation value in the velocity editor.

# Note Repeat

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(GRID) + press(Y)
```

Adjusts note repeat for the selected note in the velocity editor.

# Enter Note Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(GRID) + press(SELECT)
```

Opens the note editor menu for the held note.

# Exit Note Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
press(BACK)
```

Exits the note editor menu.

# Note Editor Cross-Screen Editing

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
press(CROSS)
```

Edits notes across multiple screens while in the note editor.

# Enter Note Row Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(SELECT)
```

Opens the note row editor menu for the held note row.

# Exit Note Row Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
press(BACK)
```

Exits the note row editor menu.

# Note Row Transpose

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
press(Y) + turn(Y)
```

Transposes the selected note row while editing velocity data.

# Add or Remove Notes with Euclidean

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(Y) + turn(Y)
```

Adds or removes Euclidean notes in the selected row while using the velocity editor.

# Change Clip Length

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(SHIFT) + turn(X)
```

Changes the clip length from the velocity editor.

# Change Selected Note Row Length

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + turn(X)
```

Changes the length of the currently selected note row.

# Clear Selected Note Row

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(X) + press(BACK)
```

Clears the notes in the currently selected note row.

# Rotate Note Row

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(Y) + turn(X)
```

Rotates the selected note row horizontally.

# Rotate Note Row from Pad Selection

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(X) + turn(X)
```

Rotates notes while pad selection mode is active.

# Quantize or Humanize Notes

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #VELOCITY

```shortcut
hold(AUDITION) + press(TEMPO) + turn(TEMPO)
```

Adjusts note quantize or humanize amount in the velocity editor.

# Horizontal Menu Item Selection - Synth

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(SYNTH)
```

Selects the Synth column item on the current horizontal menu page.

# Horizontal Menu Item Selection - Kit

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(KIT)
```

Selects the Kit column item on the current horizontal menu page.

# Horizontal Menu Item Selection - MIDI

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(MIDI)
```

Selects the MIDI column item on the current horizontal menu page.

# Horizontal Menu Item Selection - CV

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(CV)
```

Selects the CV column item on the current horizontal menu page.

# Horizontal Menu Value Change

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
turn(SELECT)
```

Changes the value of the selected item on the current horizontal menu page.

# Horizontal Menu Fine Tune

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(SELECT) + turn(SELECT)
```

Fine-tunes a horizontal menu value with the select encoder.

# Horizontal Menu Fine Tune with Shift

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + turn(SELECT)
```

Alternative fine-tune gesture for horizontal menu values.

# Next Horizontal Menu Page

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(CROSS)
```

Moves to the next horizontal menu page.

# Previous Horizontal Menu Page

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
press(SCALE)
```

Moves to the previous horizontal menu page.

# Next Adjacent Horizontal Menu

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(CROSS)
```

Moves to the next adjacent horizontal menu.

# Previous Adjacent Horizontal Menu

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(SCALE)
```

Moves to the previous adjacent horizontal menu.

# Change Keyboard Layout

#COMMUNITY #SYNTH #MIDI #CV #KIT

```shortcut
hold(KEY) + turn(SELECT)
```

Cycles through the available keyboard layouts, including the chord layouts when enabled.

# Chord Keyboard Row / Column Mode

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
hold(SHIFT) + turn(X)
```

Switches between row mode and column mode in the chord keyboard.

# Chord Keyboard Navigation

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
turn(X)
```

Moves across scale degrees in the chord keyboard.

# Chord Keyboard Navigation

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
turn(Y)
```

Moves vertically through scale degrees in the chord keyboard.

# Chord Library Voicing

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
hold(GRID) + turn(X)
```

Changes voicing for all chords in the selected row.

# Save MIDI CC Labels to Song

#COMMUNITY #MIDI

```shortcut
press(SAVE)
```

Saves renamed MIDI CC labels into the current song.

# Save MIDI CC Labels to Preset

#COMMUNITY #MIDI

```shortcut
press(SAVE) + press(MIDI)
```

Saves renamed MIDI CC labels into the current MIDI preset.

# Save MIDI CC Labels to File

#COMMUNITY #MIDI

```shortcut
press(SAVE), turn(PARAMETER)
```

Writes the current MIDI CC labels to a MIDI device definition file.

# Load MIDI CC Labels from File

#COMMUNITY #MIDI

```shortcut
press(LOAD), turn(PARAMETER)
```

Loads MIDI CC labels from a MIDI device definition file.

# Unlink MIDI Device Definition

#COMMUNITY #MIDI

```shortcut
press(SELECT)
```

Used on the File Linked setting to unlink the current MIDI device definition file.

# MIDI Follow Unlearn

#COMMUNITY #MIDI

```shortcut
hold(SHIFT) + hold(LEARN)
```

Unlearns a MIDI Follow channel or device from the current MIDI Follow submenu.

# Save Melodic Pattern

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(SAVE) + press(X)
```

Saves the current melodic pattern to a pattern file.

# Load Melodic Pattern

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(LOAD) + press(X)
```

Loads a melodic pattern from a pattern file.

# Load Melodic Pattern without Overwriting Notes

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(SAVE) + press(CROSS) + press(X)
```

Loads a pattern into the clipboard without replacing the existing notes.

# Load Melodic Pattern without Scaling

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(LOAD) + press(SCALE) + press(X)
```

Loads a pattern without scaling the original file.

# Paste Loaded Pattern

#COMMUNITY #SYNTH #MIDI #CV #KIT

```shortcut
hold(SHIFT) + press(LEARN) + press(X)
```

Pastes a loaded pattern from the clipboard.

# Gentle Paste Loaded Pattern

#COMMUNITY #SYNTH #MIDI #CV #KIT

```shortcut
hold(SHIFT) + press(CROSS) + press(X)
```

Gentle-pastes a loaded pattern from the clipboard.

# Layered Shortcuts

#COMMUNITY #GLOBAL

```shortcut
hold(SHIFT) + press(GRID)
```

Cycles through layered shortcuts when a shortcut pad has multiple actions assigned.

# Interpolation On or Off

#COMMUNITY #AUTOMATION #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(GRID_LIT)
```

Toggles interpolation in Automation View.

# Pad Selection Mode

#COMMUNITY #AUTOMATION #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(GRID_UNLIT)
```

Toggles pad selection mode in Automation View.

# Return to Song or Arranger from Menu

#COMMUNITY #GLOBAL #SONG #ARRANGER

```shortcut
press(SONG)
```

Exits a menu and returns to Song or Arranger View.

# Master Transpose

#COMMUNITY #SONG #ARRANGER #PERFORMANCE

```shortcut
press(Y) + turn(Y)
```

Transposes the song or active clip chromatically by semitone.

# Master Transpose by Semitones

#COMMUNITY #SONG #ARRANGER

```shortcut
hold(SHIFT) + press(Y) + turn(Y)
```

Changes the number of semitones used by the master transpose shortcut.
